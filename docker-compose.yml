version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data  # для сохранения базы данных
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
